# Copy this file to `.env` for local development. Never commit real secrets.
# On Hugging Face Spaces, set the same variable names under
# Settings -> Variables and secrets.

# Folium backend for geemap (also set in the Dockerfile for Spaces). Prevents the
# default ipyleaflet path from shadowing `geemap.basemaps` and breaking foliumap.
# USE_FOLIUM=1

# Full JSON of a Google Cloud service account key (one line of valid JSON).
# Paste the entire contents of the key file you downloaded from
# IAM & Admin -> Service Accounts -> Keys. The service account must have
# roles/serviceusage.serviceUsageConsumer and roles/earthengine.writer on a
# GCP project that is registered for Earth Engine. See README.md.
# GCP_SERVICE_ACCOUNT_JSON=

# GCP project id (the lowercase string from the Cloud Console, not the
# display name or numeric project number). Optional: if omitted, the project
# id is read from `project_id` inside GCP_SERVICE_ACCOUNT_JSON.
# EE_PROJECT_ID=

# --- Optional: Hugging Face Hub (not read by Home.py / Streamlit) ---
# Used only if you run manual sync commands or local scripts that call the Hub.
# For automated GitHub -> Space deploy, configure HF_TOKEN and HF_SPACE_REPO
# in GitHub Actions (secret + variable), not necessarily here.
#
# Hugging Face access token (write). Create at https://huggingface.co/settings/tokens
# HF_TOKEN=

# Space id as username/space-name (same as the URL path after /spaces/)
# HF_SPACE_REPO=yourusername/your-space-name
